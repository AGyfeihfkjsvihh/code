#include<iostream>

int main()
{
    int x = 123;
    int& y= x;
    int z =890;
    //引用在初始化之后就不能再指向其他的变量了，
    //如果可以的话，那么下面这行代码执行完之后，y的改变会导致z变量的值改变
    //但是我们从输出结果中可以看到z的值还是890，这也就印证了引用不能重新绑定这句话
    y = z;
    y = 111109;
    std::cout<<x<<std::endl;
    std::cout<<z;
}
